<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter tools:targetApi="m">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Accepts URIs that begin with "sendsay://”-->
                <data android:scheme="sendsay" />
            </intent-filter>
            <intent-filter
                android:autoVerify="true"
                tools:targetApi="m">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="app.sendsay.ru"
                    android:scheme="https" />
<!--                    android:pathPattern="/sendsay/.*"-->
            </intent-filter>
        </activity>
        <activity
            android:name=".view.AuthenticationActivity"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".services.MyReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="ru.sendsay.sdk.action.PUSH_CLICKED" />
                <action android:name="ru.sendsay.sdk.action.PUSH_DEEPLINK_CLICKED" />
                <action android:name="ru.sendsay.sdk.action.PUSH_URL_CLICKED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="SendsayExampleApp"
            android:value="true" />
        <meta-data
            android:name="SendsayAuthProvider"
            android:value="ru.sendsay.example.services.ExampleAuthProvider" />
    </application>
</manifest>
